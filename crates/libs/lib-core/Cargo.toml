[package]
name = "lib-core"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
sqlx = { workspace = true,features = ["runtime-tokio-native-tls", "postgres"] }

